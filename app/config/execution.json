{
    "default": "simulated",
    "byInstrumentType": {
        "CX": "ccxt-binance-futures",
        "EQ": "dwx",
        "FX": "dwx",
        "CFD": "dwx"
    },
    "providers": {
        "j2t": {
            "baseURL": "https://api.j2t.com",
            "accountId": "${ENV:J2T_ACCOUNT_ID}",
            "accessToken": "${ENV:J2T_TOKEN}",
            "timeoutMs": 10000,
            "defaultDuration": "gtc"
        },
        "dwx": {
            "metatraderDirPath" : "${ENV:DWX_SOCKET_DIR}",
            "confirmTimeoutMs" : "${ENV:DWX_CONFIRM_TIMEOUT}"
        },
        "ccxt-binance-futures": {
          "exchangeId": "binance",
          "apiKey": "${ENV:BINANCE_API_KEY}",
          "secret": "${ENV:BINANCE_API_SECRET}",
          "sandbox": "${ENV:BINANCE_API_TESTNET}",
          "enableRateLimit": true,
          "options": {
            "defaultType": "future"
          },
          "params": {
            "recvWindow": 5000
          },
          "symbolMap": {
            "BTCUSDT": "BTC/USDT:USDT",
            "ETHUSDT": "ETH/USDT:USDT"
          }
        }
    }
}
