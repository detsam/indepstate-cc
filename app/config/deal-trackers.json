{
  "trackers": [
    {
      "type": "obsidian",
      "vaultPath": "${ENV:OBSIDIAN_INDEPSTATE_VAULT}",
      "journalPath": "${ENV:OBSIDIAN_INDEPSTATE_DEALS_JOURNAL}",
      "chartImageComposer": {
        "type": "tv",
        "apiDomain": "${ENV:TV_API_DOMAIN}",
        "apiKey": "${ENV:TV_API_KEY}",
        "layoutId": "${ENV:TV_LAYOUT_ID}",
        "outputDir": "${ENV:TV_CHART_OUTPUT_DIR}",
        "throttlePerSecond": "${ENV:TV_CHART_THROTTLE}"
      }
    }
  ]
}
