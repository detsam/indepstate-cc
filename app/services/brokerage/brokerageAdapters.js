/**
 * Registry of brokerage adapter factory functions.
 * Keys are adapter names in lowercase.
 * @type {Record<string, (cfg: any, providerName: string, adapterName?: string) => any>}
 */
module.exports = {};
